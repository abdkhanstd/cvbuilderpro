# Cloudflare Pages Configuration for Next.js
name = "cvbuilderpro"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = ".next"

# For production deployment with custom domain
# [[pages_build_config]]
# # Build command and output directory are handled by Pages automatically
# # when connecting via GitHub

# [env.production]
# routes = [
#   { pattern = "/*", zone_name = "yourdomain.com" }  # Replace with your domain
# ]